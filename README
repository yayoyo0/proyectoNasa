In order to run  the script first instal the dependencies of the folder.

Then run the script using the comand ./nasa.py <filename>
Then check the .xls file created in the directory of the script

--V2.0
	The script is now able to work with .hdf files as long as it has writing permisions in the folder that runs
